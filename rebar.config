{erl_opts, [debug_info]}.
{deps, []}.

%% Build the Rust NIF (requires cargo, libusb-1.0 dev headers, pkg-config).
{post_hooks, [
    {compile, "native/ptlte_nif/build.sh"},
    {clean, "rm -rf native/ptlte_nif/target priv/ptlte_nif.so"}
]}.
