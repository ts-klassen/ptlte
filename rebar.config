{erl_opts, [debug_info]}.
{deps, []}.

%% Build the Rust NIF (requires cargo, libusb-1.0 dev headers, pkg-config).
{post_hooks, [
    {"compile", "bash native/ptlte_nif/build.sh"},
    {"clean", "rm -f priv/ptlte_nif.so"}
]}.
